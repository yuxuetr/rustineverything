[package]
name = "sdk-macros"
version = "0.1.0"
edition = "2021"
description = "Proc macros for the plugin SDK: hide unsafe extern \"C\" boilerplate"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
