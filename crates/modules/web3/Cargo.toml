[package]
name = "module-web3"
version = "0.1.0"
edition = "2021"
description = "Web3 内容板块（alloy / solana / substrate / 合约）"

[dependencies]
dioxus = { workspace = true, features = ["fullstack", "router"] }
serde = { workspace = true, features = ["derive"] }
serde_yaml = { workspace = true }
chrono = { workspace = true, features = ["serde"] }
sdk = { path = "../../sdk" }
widgets = { path = "../../widgets" }
app-core = { path = "../../core" }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"

[features]
server = [
    "dioxus/server",
    "widgets/server",
]
