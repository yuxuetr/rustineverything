# Burn
Burn 是一个以灵活性为核心的 Rust 深度学习框架，把「后端」抽象成可插拔组件，同一份模型代码可在多种硬件上训练。

## 看点
- 后端无关：wgpu（跨 GPU）/ CUDA / LibTorch / ndarray 任意切换
- 编译期张量形状追踪与自动微分
- 训练 + 推理一体，可导出到 WASM / no_std
- 内建指标、日志、checkpoint 的训练循环

## 适合参考
当你需要在 Rust 里端到端训练模型、又想在不同 GPU 后端间保持代码不变时，Burn 的后端抽象很有参考价值。
